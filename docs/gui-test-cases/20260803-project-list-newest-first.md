# 侧栏项目列表：最新添加在前

## 标准冒烟
- 基础界面 / 输入区 / IPC bridge / CDP 标记

## 主成功路径
- 前置：隔离数据目录 `projects.jsonl` 先写旧项目、再写新项目（时间正序 append）
- 操作：启动 GUI，读取 `#sb-projects` 下 `.sb-project` 文案顺序
- 断言：第一个项目名为新项目 basename，第二个为旧项目
- 证据：DOM 项目名数组

## 边界 / 回归
- 取消添加项目对话框后，当前选中项目不变（本轮以代码路径 + unit 覆盖；CDP 难模拟系统目录对话框）
- 对话排序不受影响：同项目内仍按 `updatedAt` 降序
