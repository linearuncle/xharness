# E2E 测试环境变量示例。
# 用法：复制本文件为 .env.test 并填入真实值；.env.test 已在 .gitignore 中，不会被提交。
# 也可以不建 .env.test，直接在 shell 中 export DEEPSEEK_API_KEY（helpers 会自动注入）。
# 两者都没有时，test:e2e 整体 skip（不 fail）。

# DeepSeek 的 Anthropic 兼容端点直接把 DeepSeek key 当作 ANTHROPIC_API_KEY 使用
ANTHROPIC_API_KEY=你的 DeepSeek key

# E2E 统一用轻量模型，成本低、可大量跑
XHARNESS_MODEL=deepseek-v4-flash
